<!DOCTYPE html>
<html>
<head>
  <title>Welcome</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
      position: relative; 
    }

    h1 {
      background-color: #007bff;
      color: #fff;
      padding: 10px;
      text-align: center;
    }

    p {
      margin: 20px;
      font-size: 18px;
      text-align: center;
    }

    a {
      display: block;
      background-color: #007bff;
      color: #fff;
      text-align: center;
      padding: 10px;
      text-decoration: none;
      margin: 20px auto;
      width: 200px;
    }

    a:hover {
      background-color: #0056b3;
    }

    .logout {
      display: block;
      background-color: #ff0000; 
      color: #fff;
      text-align: center;
      padding: 5px;
      text-decoration: none;
      position: absolute; 
      top: -10px; 
      right: 10px; 
      width: 70px;
    }

    .logout:hover {
      background-color: #ff3333; 
    }
    .center-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 20vh;
  text-align: center;
}

.round-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 200px;
  height: 50px;
  background-color: #007bff;
  border-radius: 25px;
  color: rgb(0, 0, 0);
  text-decoration: none;
  margin-bottom: 20px;
  font-size: 16px;
}

.round-checkbox-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

h2 {
  margin-bottom: 10px;
}

.checkbox-group {
  display: flex;
  align-items: center;
  justify-content: center;
}

.round-checkbox {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background-color: #000000;
  border-radius: 50%;
  margin: 5px;
  cursor: pointer;
  text-decoration: none;
}

.disable-checkbox {
  background-color: #f0f0f0;
  color: #000000; 
}

.enable-checkbox {
  background-color: #f0f0f0;
  color: #000000; 
}

.round-checkbox.selected {
  background-color: #007bff;
}

.round-checkbox:hover {
  background-color: #0056b3;
}



  </style>
</head>
<body>
  <a href="/logout" class="logout">Logout</a> 
  <h1>Welcome, <%= username %></h1>
  <p>Role: <%= role %></p>
  <div class="center-container">
  <% if (role === 'user') { %>
    <a href="/add-sensitive-info">Add Sensitive Info</a>
  <% } else if (role === 'admin') { %>
    <a href="/view-sensitive-info/admin/">View Sensitive Info</a>
  <% } %>

  <div class="round-checkbox-container">
    <h3>Poor Access Control: <%= enablePoorAccessControl ? 'Enabled' : 'Disabled' %></h3>
    <div class="checkbox-group">
      <% if (enablePoorAccessControl) { %>
        <a href="/disable-poor-access-control/<%= userId %>" class="round-checkbox disable-checkbox">Disable</a>
      <% } else { %>
        <a href="/enable-poor-access-control/<%= userId %>" class="round-checkbox enable-checkbox">Enable</a>
      <% } %>
    </div>
  </div>
</div>
  
  
  
  
  
</body>
</html>
